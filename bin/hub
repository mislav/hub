#!/usr/local/opt/ruby-1.8.7/bin/ruby

# hub(1)
# alias git=hub

require 'pathname'
$LOAD_PATH.unshift File.join(File.dirname(Pathname.new(__FILE__).realpath), '../lib')

require 'hub'
Hub::Runner.execute(*ARGV)
